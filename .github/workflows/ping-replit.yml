name: Ping Replit Bot

# Kích hoạt workflow:
# 1. Theo lịch (cron) → mỗi 6 phút ping 1 lần
# 2. Run thủ công (workflow_dispatch)
on:
  schedule:
    - cron: '*/6 * * * *'  # ping mỗi 6 phút
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Replit bot
        run: |
          curl -X GET https://215f4fdc-2c17-4ec4-862d-bc307b1e6062-00-1vqt65j23yw6b.sisko.replit.dev/